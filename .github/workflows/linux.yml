name: Linux

on:
  pull_request:
  push:
    branches:
      - master
      - main
      - dev/*

jobs:
  linux-build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v5
    - name: Build Linux
      run: ./make.sh all
